<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mydouban">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity"
            android:theme="@style/Loader"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MovieValueActivity" />
        <activity android:name=".ui.activity.MovieWebActivity" />
        <activity android:name=".ui.activity.BookValueActivity" />
        <activity android:name=".ui.activity.BookWebActivity" />
        <activity android:name=".ui.activity.SearchlnterActivity" />
        <activity android:name=".ui.activity.MusicWebActivity"></activity>
    </application>

</manifest>